// ast — abstract syntax tree node definitions
//
// defines all AST node types produced by the parser.
// uses zig tagged unions for type-safe node variants.

const std = @import("std");

test {
    _ = std;
}
